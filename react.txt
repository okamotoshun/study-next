・関数はreturnの外に記載。(中の値を使いたい場合は、useCallbackで再レンダリングさせない)
・カスタムフックスと共通化の違い => カスタムフックスの方がレイアウトのカスタムがしやすい、ロジックのみ切り出せる。
・Reactの全てのコンポーネントにuseMemo、関数にuseCallbackを使用
・some() => inclodesに置き換えれる(文字列のみの判別の場合)

 # Reactのライフサイクル
 1 DOMになる時、マウント
 2. 消える時、アンマウント
